name: pwsh-script

on:
  schedule:
    - cron: "5 16 * * *"
    - cron: "5 4 * * *"
    - cron: "5 10 * * *"

jobs:
  run:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run script
        env:
          IS_AT_SCHOOL: ${{ secrets.0 }}
          BUPT_USERNAME: ${{ secrets.2019213469 }}
          BUPT_PASSWORD: ${{ secrets.Chenchen042099 }}
        run: |
          ./main.ps1
